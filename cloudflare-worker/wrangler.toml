name = "gulf-briefing-refresh"
main = "worker.js"
compatibility_date = "2024-01-01"

# Cron trigger: 02:00 UTC = 6am GST (Gulf Standard Time, UTC+4)
# Fires the workflow even if GitHub skips the cron due to repo inactivity
[triggers]
crons = ["0 2 * * *"]
