name = "gulf-briefing-refresh"
main = "worker.js"
compatibility_date = "2024-01-01"

# NOTE: Cloudflare cron trigger removed — hit 5-trigger free plan limit.
# GitHub Actions cron handles scheduling. This worker is refresh-button only.
